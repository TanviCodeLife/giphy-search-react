import React from "react";
import styled from "styled-components";

const Title = styled.h1`
  font-size: 3em;
  margin-left: -20%;
`;

const Logo = () => {
  return <Title>Giphix!</Title>;
};

export default Logo;